import 'package:flutter/material.dart';

/// Root navigator for update prompts and in-app dialogs outside build context.
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
